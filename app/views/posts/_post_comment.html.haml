%article.comment
  = image_tag gravatar(post_comment.email_hash), :class => 'avatar'
  .comment-wrapper
    %p= post_comment.comment.html_safe
    .comment-meta
      posted by:
      = comment_author(post_comment)
      @
      = format_date post_comment.created_at

